---
interface Props {
  src: string
  width?: string
  height?: string
}
const { src, width = "auto", height = "auto" } = Astro.props
---
<video controls src={src} style={`width: ${width}px; height: ${height}px;`}></video>
