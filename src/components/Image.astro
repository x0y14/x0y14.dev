---
interface Props {
  src: string
  alt?: string
  width?: string
  height?: string
}
const { src, alt = "", width = "auto", height = "auto" } = Astro.props
---
<img src={src} alt={alt} style={`width: ${width}px; height: ${height}px;`} />
